# Infra Checks (Airflow)

This folder is reserved for infrastructure/runtime checks for the Airflow stack
(e.g., container health, compose wiring, integration smoke tests).

Code-level DAG/operator tests live in `airflow/tests` alongside the source.
