# Strava OAuth2 Credentials
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# Required: Strava API credentials (dlt naming convention with CREDENTIALS__ prefix)
# Get these from https://www.strava.com/settings/api
# These are automatically loaded by .dlt/secrets.toml via environment variable templating
CREDENTIALS__CLIENT_ID=your_client_id_here
CREDENTIALS__CLIENT_SECRET=your_client_secret_here
CREDENTIALS__REFRESH_TOKEN=your_refresh_token_here

# Optional: Override OAuth token URL (default: https://www.strava.com/oauth/token)
# CREDENTIALS__ACCESS_TOKEN_URL=https://www.strava.com/oauth/token

# Optional: Override config file location
# STRAVA_CONFIG_PATH=/path/to/config.yaml

# Optional: Set environment (development, staging, production)
# STRAVA_ENVIRONMENT=development

# Optional: Override specific settings via environment variables
# Use double underscore (__) for nested config values
# Examples:
# STRAVA__API__BASE_URL=https://custom-api.example.com/
# STRAVA__RATE_LIMITING__MAX_REQUESTS=50
# STRAVA__LOGGING__LEVEL=DEBUG
# STRAVA__LOGGING__FORMAT=json

# Optional: Sentry Cloud DSN for error tracking and observability
# Get your DSN from https://sentry.io (Project Settings > Client Keys)
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
